// Vite 项目不需要设置 __webpack_public_path__
// vite-plugin-qiankun 会自动处理 public path
// 如果需要手动设置，可以使用以下方式（但通常不需要）：
// if (window.__POWERED_BY_QIANKUN__) {
//   // Vite 使用 import.meta.env.BASE_URL，但 vite-plugin-qiankun 会自动处理
// }
